import { RandomcolorPipe } from './randomcolor.pipe';

describe('RandomcolorPipe', () => {
  it('create an instance', () => {
    const pipe = new RandomcolorPipe();
    expect(pipe).toBeTruthy();
  });
});
